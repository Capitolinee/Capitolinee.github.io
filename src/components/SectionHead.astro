---
import Icon from './Icon.astro';
import { withBase } from '../lib/url';

interface Props { title: string; href?: string; }
const { title, href } = Astro.props;
---
<div class="sec-top">
  <h2>{title}</h2>
  {href && <a href={withBase(href)}>全部<Icon name="chev" size={14} /></a>}
</div>
