---
title: 第二篇，用來看看排版
date: 2026-09-03
summary: 首頁的「最近」會把筆記、手作、書混在一起，依日期排序取最新四筆。
tags: [網站]
---

新增一篇 = 在 `src/content/notes/` 丟一個 `.md` 檔。檔名會變成網址，
所以 `hello.md` 的網址是 `/notes/hello/`。

還沒寫完的話，在 frontmatter 加一行 `draft: true`，它就不會出現在網站上。
